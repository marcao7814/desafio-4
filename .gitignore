node_modules/
dist/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
*.tsbuildinfo

# ../documentacao/ (pasta irmã, fora desta árvore de trabalho) nunca é
# rastreada por este repositório — mantida aqui só como registro dessa
# decisão, já que .gitignore não alcança caminhos fora de repo/.
